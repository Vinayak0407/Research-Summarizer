# agents/process_agent.py

def process_paper(paper_url):
    print(f"[process_agent] Processing paper from URL: {paper_url}")
    # Simulated extraction logic
    return f"Extracted text content from {paper_url}"
